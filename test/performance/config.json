{
  "general": {
    "function": "0.8 + cos(15*(x+y+z))",
    "ranks": { "A": [ 1 ], "B": [ 1 ] },
    "synchronize": false,
    "meshes": {
      "A": { "coarse": "${ROOT_LOCATION}/meshes/t_0.02.vtk" },
      "B": { "fine": "${ROOT_LOCATION}/meshes/t_0.01.vtk" }
    }
  },
  "groups": [
    {
      "mapping": {
        "constraints": ["conservative"],
        "cases": {
          "performance": {
            "kind": "rbf-global-direct", "options": "polynomial=\"off\"",
            "basis-function": "gaussian support-radius=\"0.215443\"",
            "executor": "cpu", "executor-options": ""
          }
        }
      },
      "meshes": { "A": ["coarse"], "B": ["fine"] }
    }
  ]
}
