{
  "general": {
    "function": "0.8 + cos(20*(x+y+z))",
    "ranks": { "A": [ 2 ], "B": [ 2 ] },
    "synchronize": false,
    "meshes": {
      "A": { "coarse": "${ROOT_LOCATION}/meshes/t_0.02.vtk" },
      "B": { "eval": "${ROOT_LOCATION}/meshes/t_0.01.vtk" }
    }
  },
  "groups": [
    {
      "mapping": {
        "constraints": ["consistent"],
        "cases": {
          "performance": {
            "kind": "rbf-global-direct", "options": "polynomial=\"separate\"",
            "basis-function": "gaussian support-radius=\"auto\" tuner-optimization-interval=\"once\"",
            "executor": "cpu", "executor-options": ""
          }
        }
      },
      "meshes": { "A": ["coarse"], "B": ["eval"] }
    }
  ]
}
